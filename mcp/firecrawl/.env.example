# Firecrawl MCP Server — STDIO transport
# Copy to .env and fill in values
# OR add FIRECRAWL_API_KEY directly to apps/server/.env (inherited by stdio process)

FIRECRAWL_API_KEY=
FIRECRAWL_API_URL=https://api.firecrawl.dev/v2

LOG_LEVEL=info
